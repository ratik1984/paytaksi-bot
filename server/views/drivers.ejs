<% const body = `
<div class="card"><h2>Drivers — ${status}</h2>
<table><thead><tr><th>ID</th><th>TG</th><th>Status</th><th>CarYear</th><th>Color</th><th>Balance</th><th>Actions</th></tr></thead>
<tbody>
${drivers.map(d => `<tr><td>${d.id}</td><td>${d.user.tgId}</td><td><span class="pill">${d.status}</span></td><td>${d.carYear ?? "-"}</td><td>${d.carColor ?? "-"}</td><td>${d.balance.toFixed(2)}</td><td>
${status === "PENDING" ? `<form style="display:inline" method="post" action="/admin/drivers/${d.id}/approve"><button class="btn btn-ok" type="submit">Approve</button></form>
<form style="display:inline" method="post" action="/admin/drivers/${d.id}/reject"><button class="btn btn-no" type="submit">Reject</button></form>` : "-"}
</td></tr>`).join("")}
</tbody></table></div>
`; %><%- include("_layout", { body }) %>
